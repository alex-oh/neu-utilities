function InvoiceView() {
    // Possible feature: link to detailed invoice view via /invoices/<invoice number>
    // for every single invoice listed in table
    return (
        <div>
            <h1>Invoice View</h1>
            <p>Table here with invoice properties</p>
        </div>
    );
}
export default InvoiceView;
